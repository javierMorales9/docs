---
title: "API"
description: "Uso de la API pública de Manuscritten"
---

La **API pública de Manuscritten** permite crear cartas de forma programática, tanto dentro de [**campañas automáticas existentes**](/campaigns/automated) como [**cartas individuales**](/campaigns/single).

Está diseñada para facilitar la integración con CRMs, sistemas internos o flujos personalizados que requieran enviar cartas manuscritas sin intervención manual. Puedes decargar la especificación Open API aquí

<Card title="Open API JSON" icon="file-arrow-down">
  Download the specification file
</Card>

## Autenticación

Toda petición a la API debe incluir una cabecera de autenticación con la **API Key de tu cuenta**:

```
X-API-KEY: <tu_api_key>
```

La API Key puede obtenerse desde la sección de ajustes de la cuenta:

👉 https://app.manuscritten.com/settings

Sin una clave válida, todas las peticiones devolverán un error `401 Unauthorized`.

## Welcome

There are two ways to build API documentation: [OpenAPI](https://mintlify.com/docs/api-playground/openapi/setup) and [MDX components](https://mintlify.com/docs/api-playground/mdx/configuration). For the starter kit, we are using the following OpenAPI specification.

<Card title="Plant Store Endpoints" icon="leaf" href="https://github.com/mintlify/starter/blob/main/api-reference/openapi.json">
  View the OpenAPI specification file
</Card>

## Authentication

All API endpoints are authenticated using Bearer tokens and picked up from the specification file.

```json
"security": [
  {
    "bearerAuth": []
  }
]
```